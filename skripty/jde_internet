#!/bin/bash  

sakura -h -e "bash -c 'echo ==test_pripojeni_internetu_musi_byt_===0%_packet_loss__==;wait;echo ===5_x_dotaz_na_IP_google===;wait;ping 8.8.8.8 -c 5;wait;echo ===5_x_dotaz_na_google.com===;wait;ping www.google.cz -c 5' "
